import { MetadataStorage } from '../metadata/metadataStorage';
export function Model() {
    return function decorator(object: any) {
        /**const instance = new object.prototype.constructor();
        console.log(instance.prototype);**/
    }
}